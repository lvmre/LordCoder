# Legacy Windows Helpers

These batch files are retained for compatibility with the older Windows-first workflow.

They are not the canonical runtime path anymore.

Current preferred entrypoints:

- `pip install .`
- `lordcoder --version`
- `lordcoder doctor`
- `lordcoder daemon`
